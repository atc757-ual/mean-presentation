version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mean-presentation-db
    restart: always
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
